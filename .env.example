# Optional: require one shared teacher passcode (Vite exposes VITE_* at build time).
# If unset, teachers can sign in with any passcode they type (works on Netlify with no env setup).
# VITE_TEACHER_PASSCODE=
