<?xml version="1.0" encoding="UTF-8"?>
<manifest identifier="py-lab-scorm12" version="1.0"
  xmlns="http://www.imsproject.org/xsd/imscp_rootv1p1p2"
  xmlns:adlcp="http://www.adlnet.org/xsd/adlcp_rootv1p2"
  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="
    http://www.imsproject.org/xsd/imscp_rootv1p1p2 imscp_rootv1p1p2.xsd
    http://www.adlnet.org/xsd/adlcp_rootv1p2 adlcp_rootv1p2.xsd">

  <metadata>
    <schema>ADL SCORM</schema>
    <schemaversion>1.2</schemaversion>
  </metadata>

  <organizations default="ORG-1">
    <organization identifier="ORG-1">
      <title>Cyber/AI Themed Python Lab (Grades 10–11)</title>

      <item identifier="ITEM-1" identifierref="RES-1" isvisible="true">
        <title>Python Lab</title>
        <adlcp:masteryscore>80</adlcp:masteryscore>
      </item>

    </organization>
  </organizations>

  <resources>
    <resource identifier="RES-1"
      type="webcontent"
      adlcp:scormtype="sco"
      href="index.html">

      <!-- React build files (example: adjust if your build outputs differ) -->
      <file href="index.html"/>

      <!-- If your build output is /assets/* (Vite), include the folder files in the zip.
           SCORM doesn't require listing every file, but some LMS are picky.
           If Canvas/Brightspace complains, we’ll add explicit file entries. -->
    </resource>
  </resources>

</manifest>